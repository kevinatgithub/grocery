package app.kevs.biyang.grocery

import com.bakhtiyor.gradients.Gradients

object AppConstants {
    val actionBarBackgrounDrawable = Gradients.eternalConstance()
    val containerBackgroundDrawable = Gradients.eternalConstance()
}